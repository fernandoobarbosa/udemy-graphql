type Perfil {
  id: Int!
  nome: String!
}

enum Status {
  ATIVO
  INATIVO
  BLOQUEADO
}

type Usuario {
  id: Int!
  nome: String!
  email: String!
  idade: Int
  salario: Float
  vip: Boolean
  perfil_id: Int
  perfil: Perfil
  status: Status
}
